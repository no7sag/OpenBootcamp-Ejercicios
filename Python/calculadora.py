# Complemento de "07. Modulos - Ejercicio 1"

def sumar(a, b):
    return f"{a} + {b} = {a+b}"

def restar(a, b):
    return f"{a} - {b} = {a-b}"

def multiplicar(a, b):
    return f"{a} * {b} = {a*b}"

def dividir(a, b):
    return f"{a} / {b} = {a/b}"